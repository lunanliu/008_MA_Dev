# OTA004/A03失败复核与A04修复边界

A03 prepare **NOT PASS**。Astra已独立核查原生日志、40/45实际IP、192行实际源表、退出1、6个记录身份及最后完整树空记录；未进入smoke/synth，无超时/严重内存压力。五个IP实际创建失败；不把这一失败归结为单独的TSV审计错误。

根因属于本工程移植：通用ElementTree读取/写回将XCI中厂商跨物理行的boundaryDescriptionJSON属性换行规范成空格。原件不存在INSERT _VIP、g enerated等字串，原片段逐行直接拼接为有效JSON；旧冻结副本存在错误。45份副本均发生该转换，故40个导入对象也不能认作元数据保真。

A04从已锁定原件新建45份保真副本，所有原始字节仅有既定OUTPUTDIR/PREFHDL两处标量内容替换，逆替换证明完全恢复原件。45份厂商JSON属性原字节与新副本一致；实际受管XCI还需解析完整边界JSON对象与新基线对比。原算法/端口/时钟/阈值/测试向量未改。新TSV读取以真实失败文件回归，192行均保留5列，包括1行尾空sim属性。

原根XPR引用45项完整，修复工具将在Luna原生prepare入口先按冻结基线封存XPR+45受管XCI并核对，再只替换受影响副本，保持同一个XPR。该工具尚未执行，静态证据不等于原生恢复成功；任何已有DCP或非预期编辑均阻止覆盖。原先没有成功计算阶段，不因日志重算。

保留A01/A02/A03旧输入和所有失败证据。硬预算沿用首启00:44:33 UTC，截止03:44:33 UTC，不重置。A04具体准入后由唯一Luna重试未完成prepare，随后按原门槛短测和综合。
