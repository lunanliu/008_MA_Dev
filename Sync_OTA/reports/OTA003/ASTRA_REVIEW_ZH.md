# OTA003/A01 Astra 限定接收

2026-09-17 01:50 CEST：bridge、capture、dual_rotation 三项冻结功能范围 PASS。机器复核见 ASTRA_REVIEW.json，脚本 tools/review_ota003.py。

28/28输入长度和SHA256一致；源锁 e3ea6d3fe2ee0dec000082050e44fafe77df30eb920aff98562d984c77cddcf9。实际源集10/11/15项均属于冻结清单；8/8/16参数回读、唯一PASS、result.txt、原生及下属日志均核对。三阶段保存16/19/21个PID+创建时间身份，正常退出0，无超时；最后树记录为空。独立当前工具进程扫描0，与历史树证据分开记录。

- bridge：两个独立时钟和两个来源，4个匹配事务、1个陈旧tag；背压稳定、归属路由、单信用和排空通过。
- capture：真实store/descriptor，334215个beat写入、334215个扫描读取、334215个预装读取，地址/序号/坐标逐项检查；待ACK取消不提前回收；全部读取消费且释放租约后才发布独立描述符。FE/SFO/CFO是明确接口服务模型，因此仅接收调度和存储所有权。
- dual_rotation：5120样点/2符号，两个真实坐标控制与两个真实旋转器，非零原点和两个非unity步长；1280个粗输出与1280个最终输出/标签精确比较通过，包含背压。专项配置不代表生产估计器在本TB中实际估出偏移。

包装修复：Tcl仅修改搬移后的根和verifier位置；none模式修复为无XPM实例时必须编译零份XPM。FIFO模式仍要求三份私有模型，模块名精确边界与禁止安装版绑定未放宽。原冻结verifier误将none也要求三份，属于本Astra入口假设错误；所有失败目录保留，无RTL/TB/向量/门槛改动，不重跑成功计算。

本包不构成完整OTA算法、最终XPR/DCP、综合或实现时序、持续500MS/s、NI或板测验收。具体grant SYNC_OTA_OTA003_A01_20260916T232929Z已请求总管家归还，新作业另行冻结准入。
