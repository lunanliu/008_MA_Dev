# Sync_OTA 500 MS/s 版本封存

封存日期：2026-09-19，CEST（Europe/Berlin，UTC+02）。
分支：V5.1_System_Modify。用户要求提交并推送当前仅支持500 MS/s的版本；400 MS/s方案仅作过可行性讨论，未写入RTL、IP或时钟约束。

## 固定身份

- 工程：Sync_OTA.xpr；综合顶层：sync_ota_top；器件：xcvu11p-flgb2104-2-e；Vivado 2021.1。
- 名义采样率：500 MS/s复IQ；时钟：125/150/500 MHz；输入4 lane、128 bit。
- 活动源集：rtl/sources_v51.f，共129份RTL，封存前逐份匹配 reports/v51/timing_margin20_20260918/change_manifest.json 的 active_sources SHA256。
- 粗CFO固定乘法IP常数保持1953125；前端与SFO两处相位增量常数保持295147905179；CFO参考Fs保持500000000。
- 最终输出无背压；当前帧几何、算法精度、缓存合同保持。

## 纳入本次源码封存

已完成的XPM参数兼容修复、CFO系数ROM分页面、分段缓存与局部寄存、算术/数据通路流水及控制优化，连同设计依据、静态审查和历史路径分析报告一起保留。已有证据目录中的旧源快照只作历史证据，实际生产选源仍由XPR和sources_v51.f决定。

保留20%余量实现脚本，以及2026-09-19对时钟检查的1 ps检查点表示兼容修复。该修复只接受请求周期或其最近1 ps表示，不改变真实时钟或RTL。

当前XPR已登记OPT PRE、PHYS_OPT PRE和ROUTE POST余量钩子；这属于独立Vivado实现设置。布局后的专用寄存器复制、实现策略和额外setup uncertainty不会自动回写综合DCP，也不代表LabVIEW会继承该布局布线。对NI交付仍以综合DCP和独立VHDL Wrapper为边界。

## 已知验证状态

2026-09-19 05:20 CEST的常规routed报告此前已在本任务现场核读：

| 时钟域 | 同域WNS / ns | TNS / ns | 20%余量目标 / ns |
|---|---:|---:|---:|
| 125 MHz | +0.779 | 0 | +1.600 |
| 150 MHz | +0.007 | 0 | +1.333333334 |
| 500 MHz | +0.018 | 0 | +0.400 |

该次常规setup、hold、脉宽及bus skew无违例，但三个时钟域均未达到20%余量目标。它不是NI整机时序、功能、实测持续吞吐或板测验收。

原始05:20 routed报告和DCP随后随用户重置impl_1被移除；上述数值是本任务此前核读记录，不声称本次Git提交包含该次原始报告或检查点。

封存检查时，impl_1出现新的margin20_20260919_123511_21728运行目录，日志已记录opt_design完成，尚无新routed报告。因此新版20%约束实现结果仍待完成。此次Git操作未启动、停止或重跑Vivado作业。

生成DCP、仿真波形、运行缓存及进行中的.run产物按既有.gitignore保留在本地；本次提交保存源码、配置、文档及已纳入版本管理范围的静态/历史报告。版本封存不等于500 MS/s持续吞吐或20%余量已经验收。
