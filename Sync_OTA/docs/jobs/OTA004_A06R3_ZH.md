# OTA004 A06R3：原功能重新验证与完整执行交回

## 最新授权与目标

2026-09-17用户要求：“不要去管之前这个错误的任务报告了……重新对这个任务本来需要验证的功能重新生成一次新的任务……一定要让任务的整个执行以及回传链路是能够通畅运行的……不用限制得那么死……以免Luna稍微做一点修改……不能接受回执，导致整体停工”。本任务替代旧R2的恢复流程及未执行R2I1身份专门诊断。旧记录和成功核心保留作为来源证据，不重启旧作业。

新作业由原Astra 01a0ac17-f298-7201-848c-58d09e90ebb4设计/复核、原唯一Luna 01a0ac17-4f21-7be3-8690-540c11497b1e执行。这里的“新任务”是新验证作业，沿用用户指定原配对；不创建其他执行者，不改模型/思考/速度。资源仍由总管家01a06e5a-ad5f-7bb3-9fb2-a4eb4560bbb7给新的具体准入。

目标是验证已综合真实核心的原有CDC/Gray约束与时序报告覆盖，形成可审查结果；不是只检查一个属性，也不是重新综合或重新仿真。依据各独立阶段证据给出通过、未通过或未完成，持续保留已完成成果。

## 应验证什么

| 验证项 | 证据与门槛 |
|---|---|
| 核心身份 | 固定A06R1 DCP的完整SHA256、DCP内部Top/Part/build/OOC元数据、运行期active project PART、必需核心层级、0黑盒；报告头Design必须sync_ota_top |
| 时钟 | clk125/150/500，8.000/6.667/2.000ns；不改时钟参数 |
| CDC | 同DCP完整299明细/9钟块与冻结原报告闭合；CDC1/13为0，12条CDC10只允许已审的精确复位端点；182条Warning全部OPEN，不新增豁免 |
| Gray有效约束 | 六FIFO、24宏、128目的位；源/目的原pin或cell与scope/owning cell真实对应，512对象记录；有效max_delay、datapath_only、bus-skew和逐位REQUIREMENT满足原规则 |
| 有界时序覆盖 | 9查询、最多116路径，实际端点/钟域/requirement/slack及完整原生路径报告；不能声称已穷尽2587失败端点 |

原125MHz综合WNS+2.410、150MHz-2.358、500MHz-1.279是既有未布局估计。此次报告验证不会把它变成时序闭合，更不等于实现/吞吐/NI/板测通过。

## 正确的身份依据

固定输入work/OTA004/synth_a06r1/sync_ota_synth.dcp，187800130bytes，SHA256 AC2E40A1B04CEE538F76BC5028CC8E2C3836459742D260488E231D6DD436F33E。其内部Top=sync_ota_top、Part=xcvu11p-flgb2104-2-e、build3247384、OOC=1，与实际A06R1 synth -top/-part及导出记录一致。

design.NAME只是工具设计视图的参考名，不能直接等同RTL顶层。新任务完整记录design对象的实际NAME、可用PART/TOP等属性；身份验收依据上述内容身份、真实器件/层级及报告头。存在design.PART时须与active project.PART一致。任何文件身份、内部Top/Part、实际器件或必需层级冲突均是实质HOLD，不能改预期或换文件蒙混。

## 执行步骤和失败隔离

1. 接收本任务，核对科学输入、工具、原配对及新资源/截止；确认完整回执路径已获平台认可。不存在新准入或通信审批阻塞未解决时不启动native。
2. 新空目录work/OTA004/report_a06r3，初始脚本见下方。开既定DCP一次，先做正确的内容/运行期身份核验。
3. 分阶段采集clocks、CDC、timing summary、应用XDC、9组路径、Gray有效约束。各独立阶段catch留存状态；某报告格式/API失败不阻止其它不依赖它的阶段。真实核心身份失败时停止后续设计分析。
4. 关闭设计、完成全树监管。Python严格复核单独记录；它的格式不兼容不抹掉已经生成的报告。
5. 普通执行错误由Luna自主修复并继续，只补失败/缺项。原生数据已经齐全时只离线修解析，不能为JSON或消息问题重开DCP。
6. 若必须补缺项native：在同一总grant截止内另存执行修订及新attempt，仅打开相同DCP并执行所缺阶段；已成功阶段保留引用，不重综合/IP/仿真。每次实际命令/输入/脚本指纹和修复原因记清，不按attempt重置总预算。没有足够预算或遇重大设计问题则完整交回已有成果。

## 科学输入与执行修订分开

不可自行改变：DCP/EDIF、RTL/TB/算法、定点位宽/舍入、系数/数据、器件/时钟、CDC/Gray端点及数值门槛、接受标准。科学锁OTA004_A06R3_scientific_lock.json保留原339项输入及原manifest和5项工具；不按脚本mtime选源。

Luna可自主修复：调用参数/引号、路径、私有Python/Tcl环境、启动包装、原生API写法兼容、报告字段/作用域/对象表示的等义适配、解析器、日志/JSON/Markdown回执、证据索引和交回消息。修复不得改变上述科学含义。监控器的执行兼容性可修，但不得删除全树身份跟踪、时间保护、保护用户进程或把真实内存压力隐藏。

初始执行文件另存于OTA004_A06R3_execution_baseline.json作为审查基线。需要改动时在work/OTA004/a06r3_repairs/<revision>/按tools/vivado和tools布局另存完整执行bundle，保留原文件。修订的实际哈希与基线不同不构成拒收理由；Astra检查差异是否仍属已授权执行修复。每次native前短检查相关脚本语法，记录修订原因、涉及文件、实际指纹及保持科学含义的理由，普通修复无需逐项再请示Astra。

监控器接受初始入口或上述修订bundle，自动保存actual_execution_identity.json及executed_scripts副本。Source路径允许变化，预算和科学目标不随之变化。目录/文件命名默认如下，若包装兼容问题需等义调整，Luna自修留痕，不为名字不同停工。

## 命令与资源申请

初始命令（新grant填入绝对截止后才可执行）：

```powershell
& 'C:/Users/lunan/.cache/codex-runtimes/codex-primary-runtime/dependencies/native/powershell/pwsh.exe' -NoProfile -ExecutionPolicy Bypass -File 'D:/008_MA_Dev/Sync_OTA/tools/monitor_ota004_a06r3.ps1' -Stage report -Attempt 'D:/008_MA_Dev/Sync_OTA/work/OTA004/report_a06r3' -Source 'D:/008_MA_Dev/Sync_OTA/tools/vivado/run_ota004_a06r3.tcl' -AbsoluteDeadlineUtc '<NEW_GRANT_DEADLINE>' -HardSeconds 2700
```

申请单Vivado/0MATLAB，general/synth8/8，build/IPjobs0。首次完整报告预计10—25分钟；考虑报告格式等义适配及必要缺项恢复，建议总窗口90分钟含派单、离线修复与复核交接，单次native上限45分钟且受共同绝对截止限制。首次启动窗口建议15分钟，时间由新grant明确。旧grant、旧attempt均不可复用。

工作集预计10—14GiB，仅告警；全机资源包括用户GUI/其他作业和提交余量。物理小幅不足不等待或重跑，沿用用户最新内存政策。全树PID+creation、累计后代/孤儿、截止后受控取消、最后3采样空的监管运行段与既有已审版本一致。只处理本次所属身份，不按名字批量kill，不改全局环境/分页。

## 回执范围和接收原则

用户此次明确要求完整执行及回传链路。原Luna可向原Astra发送本新任务所需的项目/产物路径、实际命令、脚本及文件哈希、原始报告/日志、成功与失败证据、修订差异和理由、进程/资源/关闭状态、剩余问题、结论边界。不把回传人为缩成某一固定句、固定字数或固定JSON字段；一次消息不足可用有序分段及项目内完整文件索引，仍只交原Astra，不借人或发外部目的地。

Astra先接收并确认收到，再做科学验收；PARTIAL/FAILED也是应接收的正式结果。回执格式错误由Astra/Luna离线修正，不能据此否认原生计算或要求重跑。正常修订不会触发“脚本哈希不同故拒收”；科学输入/门槛改变才需要Astra决策。最终PASS必须有完整原始证据，不能将“成功交回”当“验证通过”。

推荐回执有：job/attempt、实际命令/时间、修改记录、分阶段状态、产物索引/哈希、全树关闭、严格审查状态与OPEN项。可交Markdown、JSON或两者；格式本身不是科学门槛。原始材料可以完整保存在reports/OTA004及work目录，消息提供可定位索引。原Astra已有用户授权对本工程只读取证；同时遵守平台对工具消息的实际审批，不以别的渠道绕过明确拒绝。

正式启动前要实际验证通信；任务書的授权说明不能替代平台审批。基础固定文字往返已成功，但2026-09-17T12:13:56Z的新任务路径/计划预检被自动审批拒绝。此阻塞属于工具审批，不是Luna执行能力或工程功能失败；未声称完整证据链畅通。需要平台认可该具体原配对回传后才能正式开工，禁止借更换目的地/模型/通道绕过拒绝。

## 保留与不扩展

原三个工程只读；不重跑成功smoke/IP/综合，不implementation/全帧/扫描，不生成CLIP XML。新作业覆盖原报告验证功能，真正RTL时序修复在取证后另作设计与必要短验证。旧失败和未执行诊断只作为历史，不再成为本任务必须逐个补跑的步骤。
