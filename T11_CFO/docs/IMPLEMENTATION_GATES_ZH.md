# 实现顺序与验收边界

1. **来源与独立工程**：完整批准MATLAB版本/参考迁移并逐文件hash；FLGB、Vivado2021.1、显式source set/top/XPR、可由GUI打开、相对工程依赖、本地Git隔离。实际工具导出源集须与锁定表对比。当前已完成磁盘副本；真实XPR创建待Luna。
2. **四路复旋转核**：2-symbol真实片段和边界向量逐bit一致，负频率、RNE、饱和、连续II=1、背压、复位/abort、元数据拒绝。首包只此算术核；不包含估计器，phase字外部配置。
3. **坐标控制器**：按实际Q28两级步长/raw_origin/粗Hz及输出原点形成冻结模型相同48位相位字，再RNE到32位。控制链逐整数复核，保留两次S16量化。
4. **74点估计后端**：相关输入定点→CORDIC相位→FFT256质量门/相位OLS→Hz/相位控制；完整74点，不用2点代替。MAIN、FFT fallback和INVALID的判据逐字段核对，不能参考答案驱动DUT。
5. **必要FFT前端**：2048FFT、820导频乘积和累加、74个符号地址/序号，严格对应批准自定义bittrue缩放。官方IP若数值规则不同须明示并审查，不冒充厂商bittrue已验证。
6. **缓存和完整链**：在明确内存/端口/在途预算内实现单CFO环候选；已发布T10模型波形和批准nominal波形回放，计数/错误定位/两次量化逐点一致。只补阻断点所需验证，不默认展开新的CFO/SFO扫描或长回归。
7. **综合和必要物理验证**：真实FLGB资源、150 MHz时序和600 MS/s瞬时/500 MS/s持续服务分别记录；缓存寿命、任意允许突发/停顿范围和CDC须闭合。算法研究通过不等于正式T11/T12/T13 PASS。

所有原生任务均由本T11唯一Luna执行。Astra冻结每个命令、实际输入指纹、输出目录、总机资源预算、预期时间、硬超时和异常保护后可直接准入普通Vivado步骤；不再逐步向总管家重复索要许可。MATLAB仍必须获得既有中央槽精确grant。T10占一个Vivado主作业时，CFO最多一个，重步骤串行。原始失败证据保留，只重试受影响失败阶段。