# 同步前端：自主捕获、完整核心工程与独立 VHDL 接口交付

用户最新交付范围（2026-09-15）：仅交付完整 Vivado 核心 RTL 工程（top=sync_frontend_top）、独立明文 VHDL wrapper、可复现输入/预期输出及中文手工操作说明。CLIP XML 与 LabVIEW CLIP 配置由用户亲自创建；Agent 不新增/生成/修补 XML，不创建 LabVIEW 项目，不做 wrapper 回填或独立 wrapper 网表验证，不为交付重跑冻结实验。

授权：2026-09-14，用户在总管家01a06e5a-ad5f-7bb3-9fb2-a4eb4560bbb7明确批准修订方案并要求立即建立Astra/Luna配对，尽快完成综合和CLIP导出。本文覆盖先前仅用预切3176点前导的板测方案；原工程只读保留。

## 目标与边界

新工程D:/008_MA_Dev/Sync_Frontend，目标xcvu11p-flgb2104-2-e，Vivado2021.1。本任务负责连续原始I16/Q16输入 → 前导候选检测与捕获 → 粗TO/粗CFO → 局部CFO补偿与精TO → 有效帧标记/结果/原始缓存定位 → 用于用户手工 CLIP 集成的完整工程、VHDL 接口及测试资料交付。TO是时间偏移，CFO是载波频偏。全帧CFO补偿、SFO估计/重采样、后续CFO和NI平台VI由各自范围承担，本任务给出连接合同及板测指南。

外部输入不得要求已知frame_id、真实frame_start、相对真实帧起点的sample_index或oracle裁切。允许时钟、复位、数据有效/就绪、采集会话控制和真实数据中断标记等传输信息。接收端内部对有效样点计数；检测产生候选编号，确认后产生本机接收帧号，不声称解码了发送端协议帧序号。

必须在检测前保存真实历史数据。滑动检测、候选确认、去重、拒绝/重捕获、缓存保护/回收、原始坐标到局部窗口坐标转换均属本次实现范围。随机或未知前置长度不能靠TB给出答案。现有T04的index0触发和frame_id是局部窗口合同，不能冒充连续捕获；仅包装自动编号不构成合格。

优先保留S&C粗同步及T05现有定点算术/官方IP。可增加或调整必要的捕获和控制结构以满足批准的自主输入范围，所有功能变化与命名/排版分开。若有限窗核无法仅靠外层控制达到持续处理要求，应明确分析并实现必要控制修改，不重复请示本授权已包含的工作；不得静默更换核心算法、位宽或放宽已有适用域精度。

## 文件与版本

独立本地Git，不设置云remote。固定目录：rtl/frontend、rtl/timing、rtl/carrier、rtl/buffering、rtl/common；ip；constraints；sim/tb、sim/data、sim/waves；matlab/waveform、golden、bit_true、comparison；vivado/Sync_Frontend/Sync_Frontend.xpr；docs；tools；reports；release；必要work。Vivado实际工程是唯一有效编译源集，不能另建隐藏拼接入口。生成物留在自己的工程/work，不充斥源码目录。

从原T04/T05正式V2验收manifest选完整源/IP/ROM/配置；先读原docs/engineering/T00_T06_BASELINES_ZH.md及工具调用指南。旧Preamble Synchronization只是未验证组合参考，不能整包当生产基线。旧FLGC报告只作历史，不能改标签冒充FLGB通过。

新模块、文件、实例和自有接口使用功能名，例如sync_frontend_top、sync_coarse_acquisition、to_coarse_metric、cfo_coarse_estimator、cfo_preamble_rotator、to_fine_estimator、sync_result_join、sync_frontend_clip。T编号仅作来源导航。厂商加密/生成代码不手工替换内部名，功能包装和来源映射说明例外。

统一缩进、端口和逻辑换行，保留符号、运算顺序、非阻塞赋值、位宽、舍入、饱和、时序/复位/握手语义、宏/属性。原样迁入、重命名排版、功能封装分开Git提交。保存源映射/哈希及实际选源，做重命名还原后的结构/词法核对和必要短I/O对照，不能以空白diff或编译成功自称等价。

只读原项目和T10/T11。新工程构建不得依赖旧绝对路径。MATLAB来源、冻结波形、定点参考、初始化文件全部有独立可定位副本及入口；不为整理重算已经成功的大矩阵。

## 时钟、缓存与接口

首选125MHz单域、4复数样点/拍；算法物理Fs仍500MS/s。NI提供合格125MHz逻辑时钟即可，不无故引入MMCM/500MHz。若必须使用不同输入时钟，明确MMCM/时钟缓冲/复位锁定和CDC所有权并验证，不能只声明create_clock。

现有局部估计窗口联合需真实样本[-256,2919]共3176点；16KiB是预装局部核测试基线，不能直接当连续捕获缓存容量。新连续缓存按检测延迟、候选等待、在途读写和历史保留核算，避免盲目加整帧URAM/DDR。说明缓存是否仅前导、如何给下游提供绝对样点位置和采集代次；保留同帧粗TO/CFO配对、精TO最终坐标含义及原始数据未做全帧CFO补偿的事实。

CLIP明确原始数据加载/连续流、控制、结果读取接口。跨域只用正确消息/FIFO结构，不能逐位同步多位数值；没有第二域就写明同域连接条件，不加虚假CDC。异步控制和reset需同步。真实500MS/s不可反压的采集源与可反压DRAM回放分开说明；必须核对有效样点计数、丢重乱序、候选服务/队列容量，不能把4x125直接当持续资格。

## 最小验证及交付门

先给出简洁架构/接口/来源和第一包，尽快形成可打开XPR；Astra自行冻结派给本任务Luna，不等用户重复批准。短测试应覆盖：未知前置长度与四lane不同落点；跨加载块的前导；噪声/无前导及无效候选后重捕获；至少两次有效检测的内部编号和坐标；必要原适用域正负TO/CFO；结果读取停顿、复位/中止和输入中断的定义。真实起点仅允许进入被动checker，不得进入DUT/驱动控制。保留精度及拒绝条件原有边界，不把有限用例当任意信道证明。

原核等价检查、真实联合行为检查、正确FLGB综合、125MHz独立布局布线及时钟/复位/CDC检查分列。避免长帧与同源125/150重复仿真；已有成功证据直接复用。第一次确定性错误立刻记录数据/坐标/帧状态并结束该失败尝试，不等长任务结束才查错。

最终仅交付：①可直接在 Vivado GUI 打开的完整 RTL 工程，算法核心为综合顶层，约束/IP/TB/已完成证据齐全，用户可手动综合导出 DCP/其它网表；②按核心接口提供独立明文 VHDL wrapper；③可复现输入、预期输出及中文手工测试说明。静态核对实际选源、端口、依赖、SHA；不要求 Agent 生成 XML 或配置 LabVIEW，不以 wrapper 为顶层生成交付网表，不重跑已冻结实验。

提供中文本科生项目书、Vivado GUI指南、时钟/端口/位宽/IQ顺序/单位/坐标/握手说明、MATLAB黄金/bit-true入口、板测输入与预期结果、读回比较脚本、失败码和最小调试信号。用户负责后续LabVIEW VI搭建、NI全目标编译和板测，本任务交付上述三类资料，用户亲自在 LabVIEW 创建 CLIP XML/配置；不可声称已完成用户板测。

## 配对、资源与沟通

新配对：GPT-6 Astra High负责设计、冻结、复核、发布；GPT-5.6 Luna Max负责工具执行、监控、执行问题自修。两者Standard，禁止Fast；正常后续消息不得覆盖model/thinking/service_tier，不改全局。工具无速度字段时记录请求值与核实状态，不伪称已设置。

T10原全帧正常运行不暂停、不设墙钟截止。全机最多2个Vivado主作业，每对最多1个；T10保留现有槽，前端与T11共享另一个槽，新前端优先，已运行T11阶段不中断。共享槽所有权由总管家记录，禁止各对同时依据“看到空闲”自行抢占。未获得本对slot时可设计、整理、离线核查，不启动Vivado。所有MATLAB仍使用原唯一串行槽，先查占用。

新作业general.maxThreads=8、synth.maxThreads=8通过run PRE传入子进程，jobs/xelab默认上限16；按实际总资源调节并记录，内存估算告警不直接终止。新MATLAB无-singleCompThread。Luna自修路径/命令/私有环境/包装，不能改科学输入或RTL；重大问题和完成才去重交回唯一Astra。Astra派单后结束轮次等待，不空转；Luna负责持续运行期间监控。

各任务仅工作期间每15分钟按既有表格报当前订阅周配额及调度，不报token/模型调用量；空闲停表。总管家原自动化15扩展监督本配对，检查漏派单/漏交接/无人监控并去重补接，不新建值班员。完成源码/导出各阶段及时发布证据，避免无限扩展验证矩阵。
