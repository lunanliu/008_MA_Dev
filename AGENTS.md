# 本工程执行规则

1. 用户总管家任务的最新要求优先。范围为T10两级SFO独立Vivado工程；器件只用 `xcvu11p-flgb2104-2-e`。暂不开发CLIP/LabVIEW或CFO后续模块。
2. 开工先读 `README_ZH.md`、`docs/PROJECT_SPEC_ZH.md` 和 `docs/AGENT_GUIDE_ZH.md`。本目录是独立副本，禁止引用或修改旧工作区来完成构建。
3. 唯一硬件顶层、仿真顶层及准确源集由Vivado工程和原生Tcl共同定义。禁止递归收集整个目录、按mtime挑选同名源码、静默替换IP/参数/参考答案。
4. 原T10 Astra规划代码并复核，唯一配对Luna执行MATLAB/Vivado和自修执行问题；模型/强度/速度保持用户设置，Astra禁Fast。单写者，不跨任务借用进程。
5. 每项原生作业先固定输入、预算、硬超时和结果目录；默认1 MATLAB＋1 Vivado主作业。后续综合/实现统一显式使用 `launch_runs ... -jobs 16`，GUI的Number of jobs选16；仿真展开子编译统一 `xsim.elaborate.mt_level 16` / `xelab --mt 16`。同一工程内工具派生的并行子作业获准，但须计入总内存预算；不代表16个跨任务实验同时启动。jobs不是单作业线程数，保留工具自身限制。若资源实测不足，报告事实，不静默降并行数。先检查数据加载/首错，再长跑；不因日志或交接问题重跑成功计算。
6. 运行产物留在 `work/` 或Vivado自身运行目录。禁止改全局环境；Python用独立解释器，避免继承Vivado的Python库路径。正常GUI仿真不依赖旧Python监管器。
7. 工作中的任务每15分钟报告进展、耗时、进程/队列和当前账户周配额；空闲停报。沿用总管家已有调度，不新建重复唤醒链。
8. 原件及失败证据保留。修改前确认Git状态、来源和依赖；仅提交已审阅的本次文件。本项目仅本地Git，不设置或推送云端remote。
9. 工程能打开、仿真通过、实现时序通过是不同验收。旧FLGC结果保留历史身份；任何新FLGB通过声明必须对应实际工具证据。
