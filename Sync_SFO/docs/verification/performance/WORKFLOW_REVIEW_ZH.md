# 本次小探针：结果与执行流程复核

2026-09-14。范围是Vivado 2021.1的线程配置、同一8位计数器的综合/布局布线/报告，以及MATLAB R2024a的自动计算线程。没有运行真实T10或XSim仿真，不能据此宣布T10、板级时序或仿真加速通过。

## 最终结果

| 项目 | 已得到的证据 |
|---|---|
| 综合 | 同一tiny工程完成综合，日志明确最多8个并行进程 |
| 布局与布线 | 从原优化DCP恢复，最终place/route均成功；10条可布线网络全部完成，布线错误0，DRC违例0 |
| 线程设置 | place、route均明确最多8 CPUs；打开DCP曾恢复默认2/4，阶段钩子重新设置为8/8 |
| MATLAB | 正常启动与automatic回读均8，16.097秒完成；没有创建并行池 |
| 最终续跑 | 157.245秒，退出0，所属进程全部结束；已保存placed/routed DCP及报告 |
| 内存 | 最终进程组工作集合计峰值5.415 GiB；系统可用内存最低12.901 GiB；4 GiB告警触发后继续且完成 |

4 GiB原本只是小探针的内存预估，把它当硬终止线是本次规划错误。用户纠正后改为软告警，按实际系统可用内存判断严重压力。微小超出预估不应造成杀进程和重新安排；未来真实T10预算不能照搬这个4 GiB数字。

最终恢复采用同一优化检查点的Non-Project原生命令，保留原.xpr和旧impl_1中断记录，没有伪造run管理状态。小探针缺少板级时钟来源和端口物理位置约束；报告中的OOC时序警告保留，不能将其中正裕量当作真实T10时序闭合。

## 哪些执行规范仍未落实好

| 实际问题 | 影响和处理 |
|---|---|
| 任务权限与新目录未对齐 | 旧Luna的workspace-write不包含新根目录，apply_patch写入失败；其它任务的完全访问不会自动传递。启动前应由实际执行任务验证写入能力。Astra后续也遇到沙箱准备失败，按既有授权使用允许的单次调用权限后继续，没有改全局权限 |
| Vivado属性传参错误 | 以连字符开头的值被当成选项；在综合之前退出。只修复失败入口，保留成功MATLAB与参数查询 |
| 重现已知redirect错误 | 本机不支持该命令；停止重复帮助查询，直接输出原生日志。指南存在不等于执行时已落实 |
| 已派发输入被原地改动，时间记录错误 | 帮助实际01:29:27退出，随后01:29:39记录却称“未启动”。原记录与ZIP保留，追加审计用实际日志纠正；后改哈希不能冒充本次执行输入 |
| 错把会话名当RTL顶层 | DCP打开后NAME为checkpoint_tiny_opt；内部元数据的Top=tiny及part/哈希正确。改为核对真正的输入身份 |
| source变量污染与日志中断计算 | child_hook覆盖并关闭主脚本同名文件句柄。一次place已成功，却在保存DCP前因可选日志失败退出；没有placed文件可恢复，故最终确实补算了一次place。这是脚本缺陷造成的额外工作，不能声称零重复 |
| 修复单写者一度重叠 | Astra与Luna同时准备同名修复，存在文件断言阻止覆盖；随后撤回Astra替代入口，仅由Luna修复和执行 |

总管家自己的只读查询/文档维护也出现过PowerShell语法、目录和补丁上下文错误，均只修复相应操作，没有据此启动或重跑原生实验。

## 已得到证据的正确行为与后续要求

- MATLAB、综合和优化没有重跑；必要的place重算原因公开保留。失败日志、原始输入和修复版本保留。
- rev04实际修复了句柄同名冲突并成功保存DCP，但仍在检查点前写事件，尚未普遍隔离或降级可选日志失败。这项顺序与错误处理要求仍需在后续实际使用的入口落实，不能声称本次已完全修好。
- 软内存告警允许计算完成；进程归属、实际资源和退出状态均有记录，没有按名字批量终止其它程序。
- 派发后的文件一律视为冻结。普通执行修复由Luna单写，Astra复核；不能凭界面未显示启动而假定尚未执行。
- 小型检查先使用已有稳定原生命令，不为帮助或日志重新建立复杂框架。尚未触发的Job构造异常记录、外部PID创建时间校验不足留作后续入口改进，不能伪称本次已验证。

最终判断：本次规定范围内的原生计算和配置核对完成；执行过程存在多项真实缺陷，尚不能称工具调用流程完全可靠。主要额外等待来自准备、权限、调用脚本和交接，而不是单纯CPU线程不足。规范是否改善应在下一项已经需要的工作中核验，不再为本报告追加实验。

证据见 `native_probe_summary.json` 和 `native_probe_evidence.zip`；保留完整命令、输入指纹、日志、原始失败和最终检查点。旧工作区和用户GUI保存的工程改动未混入本次性能配置提交。
