# T11～T13 CFO RTL 开发范围（2026-09-14）

用户已在总管家任务批准本方案，并在原 T11 Astra 任务直接解除此前 CFO 暂停限制。T11 的直接授权原文保存在 D:/008_MA_Dev/T11_CFO/docs/provenance/DIRECT_USER_RESUME_20260914_ZH.md。旧 CFO 暂停记录保留历史身份，不再作为本范围的启动阻塞。

1. T10 在 D:/008_MA_Dev 继续现有完整帧仿真；T11～T13 在 D:/008_MA_Dev/T11_CFO 独立推进 CFO RTL。器件固定 xcvu11p-flgb2104-2-e，不恢复 CLIP/LabVIEW、旧 runner 或其他任务。
2. 唯一 T11 配对：Astra 01a076f1-ad1b-7d33-a29c-4ea34ae84d01 负责设计、冻结作业、派单和复核；Luna 01a076f0-d8c0-72a0-8371-cb2ff29c1b28 负责执行、监控和普通执行故障自修。保持用户模型、强度和速度，Astra 禁 Fast；不跨任务借执行者。
3. 从已冻结 CFO 黄金模型和定点模型迁移，保留参数、参考来源及适用条件。完善粗 CFO 补偿、残余 CFO 估计和最终补偿的 RTL；CFO 补偿是复数相位旋转，不改变采样网格。
4. 工程供人类直接在 Vivado 打开：明确 XPR、硬件/仿真顶层、源集、IP、约束和 testbench；MATLAB 模型、波形来源及中文说明可定位。单一版本整包选源，构建不依赖旧可写工作区。沿用本地 Git，仅提交本任务文件，不云端推送。
5. 优先交付整条链路的缓存容量、数据保存时间、读写端口和带宽总账。共享缓存须证明最后一次读取和在途数据已安全结束；不能把 T10 已用于帧间重叠的缓存当作空闲存储。运算核开发可先行，完整缓存和持续吞吐结论须有实现证据。
6. 原 Astra 对每项新作业冻结命令、输入/源码哈希、实际工程选源、输出目录、预计资源/耗时和异常保护后，可直接派原 Luna。首包 CFO-NATIVE001 以 T11_CFO/docs/ROTATOR_NATIVE_JOB_001.md 及其锁定清单为准；消息送达、原生启动和通过验收分别记录，不因重新授权改写冻结首包。
7. 全机最多两个 Vivado 主作业：T10 保留一个，T11 最多一个；T11 重步骤串行，启动前核算全机资源。MATLAB 始终共用唯一中央串行槽，首包不使用 MATLAB。不能接管或终止 T10，也不因估算小幅越线停止有效计算。
8. Vivado general/synth 线程请求 8，构建 jobs 与 xelab 子编译默认/上限 16，经真实子进程核验；资源不足才有据下调。T10 不设五小时墙钟截止；T11 按具体作业的阶段保护执行，两者不能混用超时规则。
9. 首包短旋转测试与后续估计器、整链、综合时序、持续吞吐分别验收，不把局部通过说成全链通过。已成功阶段不重复；普通执行问题由 Luna 自修，设计/结果根本问题才交 Astra。完成后由原 Astra 复核再规划下一包。
10. 工作期间每15分钟沿用既有表格报告，空闲停报。总管家自动化15监督两条已授权链路，正常等待不唤醒 Astra；两项范围均完成或用户停止后再收尾。不建立新的值班员或重复唤醒链。

