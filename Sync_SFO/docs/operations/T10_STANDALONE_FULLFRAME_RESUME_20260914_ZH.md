# T10新工程整帧行为仿真恢复授权

用户2026-09-14最新明确恢复本项，并澄清五小时只是暂时无法回复的窗口，正常仿真超过五小时仍须继续运行和监控，不能到点中止。此授权优先于旧暂停及旧准入时间限制。

1. 工程 D:/008_MA_Dev/vivado/T10_SFO/T10_SFO.xpr；器件 xcvu11p-flgb2104-2-e；硬件顶层 t10_two_pass_system；仿真顶层 t10_full023_tb。使用当前冻结frame6001完整522符号输入，真实两次SFO估计与两次重采样，保存全帧结果并复核。名义1,336,320个复数点、74个估计窗口；参考值只作比较。
2. 用户离线窗口约为2026-09-14 01:57:32至06:57:32 UTC（柏林03:57:32至08:57:32）。这不是启动许可到期、进程终止或监控结束时间；无回复仍继续已授权工作。absolute_simulation_deadline_utc=null，禁止继承旧runner的墙钟截止。TB原有16ms模拟时间看门狗是电路功能检查，不是五小时墙钟限制；保持其含义。
3. 唯一原配对：Astra 01a08299-4a14-7ef0-9000-50ec54cfdce2 规划、必要工程修改、具体作业冻结、准入、派单及结果复核；Luna 01a08298-b5b6-79b3-b5b4-18b8ac68993a 执行、监控、自修普通运行问题。模型/强度/速度保持用户设置。Astra可在本范围完成具体冻结及准入后直接派单，无需再等总管家或用户逐项签回。
4. 启动前冻结命令、工具版本、源码/IP/输入哈希、XPR实际选源、资源预计、异常保护和独立结果目录。保留用户GUI对XPR的修改；本次已审阅身份写入独立清单，验收器显式使用该清单，不能改回用户工程或自动重写旧清单掩盖差异。派发后输入不原地改，修复另存版本，单写者。
5. 使用同一新工程的Vivado原生流程。允许必要FLGB IP行为产物生成、编译、展开及最小入口修复，必须运行到完整TB完成，不停在0ns或短前缀。行为仿真不以重做完整综合/布局布线为前提。已完成小性能探针不重跑；身份一致且有效的新工程产物可复用，旧FLGC缓存不冒充新结果，中断XSim不假称可恢复动态状态。
6. 性能 general.maxThreads=8、synth.maxThreads=8；构建jobs与xelab子编译默认16并回读真实参数。默认一个Vivado主作业，其必要子进程充分并行；资源不足才有据下调。XSim主运行不承诺16核线性加速。本次不增加在线MATLAB依赖。
7. 内存估算只作告警，不以4GiB或其它估算越线终止。监控全部所属子进程、系统可用内存及提交压力。只有明确错误、实际严重资源压力或结合模拟进度/CPU等证据确认异常停滞才执行已记录保护流程；不能仅凭日志不变或五小时已过判断卡死。只处理本作业进程树，不关闭用户应用，不重启电脑。
8. 首先核对数据加载和首段真实诊断；首个真实功能错误及时保存证据并交Astra复核，避免无意义等全帧结束。普通路径/参数/环境/报告问题由Luna自行修复，只重试失败阶段。日志/交接等可选环节失败不能使成功计算丢失或重跑。
9. 验收沿用项目书与TB：逐I/Q分量误差不超过1 LSB、最终334,080拍、74窗口、估计值、故障及顺序检查。FFT有效窗口EVM低于-45dB，全时域EVM测试报告而不采用同一硬门槛。单帧结果不宣称连续多帧吞吐、FLGB实现时序或板级验收。
10. 保存完整原生日志、实际编译列表、结果/事件/点级/诊断记录及必要波形，避免全层级及大存储器无选择写WDB。最终短中文报告说明完成范围、功能误差、耗时、峰值资源和剩余问题。本地Git只提交本次审阅文件，不云端推送。
11. 总管家自动化15每15分钟检查派单、运行监控、完成及复核交接，正常等待不唤醒Astra。各任务只在工作期间按既有表格报告。五小时后继续监督直到本项完成或用户明确停止。重大方案变更确需用户决定时，保留成果和仍有意义的有效运行等待回复。

不恢复CLIP/LabVIEW、CFO、旧runner或其它实验，不擅自扩大研发目标或放宽验收。平台权限审批仍按实际能力办理，不能宣称绕过。