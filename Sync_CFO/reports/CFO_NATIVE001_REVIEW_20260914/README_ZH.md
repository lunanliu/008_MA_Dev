# CFO-NATIVE001 独立复核：算术通过，取消时限未通过

2026-09-14。精确复核对象为 attempt_20260914T112107511Z_luna，完成清单SHA256 E4CD04723695F2C493F14834120C419CFDD842D64D1D2EBCA892637BA6CB1035。机器结论与核查代码同目录。

## 可以接受的结果

39个冻结源/配置身份相同，实际Vivado导出17个工程成员一致，器件FLGB、Vivado2021.1、xelab实际--mt16。独立从输入整数、批准ROM和相位参数重算20480个复数样本，共40960个I/Q分量，与RTL逐点零差；另外核对元数据及饱和标识。边界case有5040个分量饱和，正负RNE和相位地址半格均覆盖。

四个case各1280拍。无背压case输入/输出II=1，延迟5拍；其余case实际输出背压659/1202/1205拍，冻结TB的保持断言未报错。reset、abort各丢弃4个在途拍，新generation无旧数据；错误帧序号和无效采样数被拒绝。

因此验收 **四路旋转算术与协议的限定行为里程碑**。不重跑这批已经成功的计算。该结果不含坐标控制器、74点估计器、全帧、综合、150MHz物理时序或持续500MS/s资格。

## 必须保留的执行缺陷

取消探针11:28:28.499937 UTC创建原生Vivado，11:29:02.611才请求Ctrl+C。11:29:42.393请求首次强制收尾，已超过从原生启动起60秒；该脚本又因foreach变量$pid与PowerShell只读$PID冲突而失败。11:29:48返回tree_empty=false。后续引号/编码修复持续到11:32之后，最终记录证明已释放，但不能把它说成60秒保护或ready后3秒完整收尾通过。

以上时间来自原Luna会话的本次原始工具记录，CANCEL_RESULT中的最终成功概括遗漏了这个区别。当前没有证据证明硬时限和严重内存保护在无模型消息时自动运行。下一步必须用独立监管器和本次拥有的Windows Job内核对象管理子树，在launch前安装保护，按自身单调时钟触发与确认结束；不能在超时后才请求模型生成kill命令。

这是执行保护缺陷，不否定已经归档且独立核验的旋转数值。只重验受影响的新入口正常退出、错误退出和自动取消，不重复RTL计算。

## 资源与工程证据的边界

原生日志显示建工程11秒、编译展开与短仿真23秒；Vivado父进程报告峰值1131.988MB。未保存完整所属子树的峰值内存，不能将父进程值说成整机或子树峰值。下一入口须记录系统可用内存及作业内存口径、活动进程数、阶段时钟和退出码。

XPR在运行时被Vivado更新：仿真前0FCD51054BC8AE24B2BCFEC1E4BDB6516BBF1F9D0AA480E7A3BEDDD151E64ED9，仿真后/取消准入8EE3459986419C34B42B4261A0E38B2D5C670B47E25E33D80B46074CD4EE8063。实际源集不变。不能将两者默认为同一指纹。run线程hook未登记utils_1的原生警告保留；新一版工程应将hook纳入工程支持文件，不能声称当前Archive Project可独立带齐hook。没有IP的初始旋转工程缺省.gen目录警告不影响此次逐点结果。

完整工程与算法源不改，下一包CFO-GUARD002先修执行保护；随后继续已批准的坐标控制器和74点后端。